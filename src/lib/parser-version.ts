/**
 * Bump when SAC JSON extraction / semantic counters change (impacts readiness inputs).
 * Stored on each asset so score drift can be tied to parser updates vs. client data.
 */
export const PARSER_ENGINE_VERSION = "bdc-pulse-parser-v3";
